# Server
PORT=4000
NODE_ENV=development

# Database PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=shortlink_db
DB_USER=shortlink_user
DB_PASSWORD=shortlink_pass
DB_DIALECT=postgres

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# ElasticSearch (for Docker Desktop)
ELASTICSEARCH_URL=http://localhost:9200
ES_HOST=localhost
ES_PORT=9200
REQUIRE_ELASTICSEARCH=false
# Remove these lines if using Docker Desktop default setup (no auth)
# ELASTICSEARCH_USERNAME=elastic
# ELASTICSEARCH_PASSWORD=password

# JWT
JWT_SECRET=Gx3d$uD9q!Wv7zT*Jm#L8sP@A1Rc6nEo
JWT_EXPIRES_IN=24h

# Google OAuth (Replace with your real Google OAuth credentials)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:4000/api/auth/google/callback

# URL Validation
GOOGLE_SAFE_BROWSING_API_KEY=
VIRUSTOTAL_API_KEY=

# Application
APP_NAME=Shortlink-System
APP_URL=http://localhost:3000
API_URL=http://localhost:4000